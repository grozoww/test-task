import type { Size } from './types';

export const MIN_NOTE_SIZE: Size = { width: 120, height: 90 };
export const DEFAULT_NOTE_SIZE: Size = { width: 180, height: 150 };
export const MIN_DRAW_SIZE = 24;
